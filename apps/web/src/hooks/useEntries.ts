// Temporary shim to avoid breaking imports while refactoring
export { useEntriesList as useEntries } from './useEntryQueries';
// TODO: Remove this shim after all components migrate to the new hooks.
