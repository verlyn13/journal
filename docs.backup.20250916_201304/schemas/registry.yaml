# Schema Registry for Journal Documentation
# Tracks schema versions and migration paths

current_version: v1

versions:
  v1:
    released: "2025-09-16"
    description: "Initial schema version with base, API, and ADR schemas"
    schemas:
      - name: base-document
        path: v1/base-document.schema.json
        description: "Core schema for all documentation"
      - name: api-reference
        path: v1/api-reference.schema.json
        description: "API endpoint documentation schema"
      - name: architecture-decision
        path: v1/architecture-decision.schema.json
        description: "Architecture Decision Record schema"
    migration:
      from_version: null
      breaking_changes: []
      migration_guide: "Initial version - no migration needed"
    features:
      - Structured metadata with frontmatter
      - Semantic chunking support
      - Relationship tracking
      - Quality metrics
      - Validation support